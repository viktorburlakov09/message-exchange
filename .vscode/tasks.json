{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build C++ Project",
            "command": "cmake --version && rm -rf build && mkdir build && cd build && cmake .. && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Task generated to build core_lib, consumer_app, and producer_app via CMake."
        }
    ]
}
